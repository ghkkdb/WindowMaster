# WindowMaster 完整可用 MVP 方案

## 1. 目标与边界

WindowMaster 是 Windows 10/11 64 位桌面窗口增强工具，基于 Python 3.11、PySide6、pywin32 和 psutil。首版提供窗口列表、透明度、置顶、左右分屏、最大化、最小化全部程序、可自定义全局快捷键，以及多套命名布局保存与恢复。

首版包含可选系统托盘和快捷键自定义，但不包含自启动、自动更新、目标窗口隐藏或自动启动缺失应用。目标窗口隐藏暂缓，避免用户无法找回目标窗口。

## 2. 架构

```text
main.py
├── ui/main_window.py          PySide6 主界面、状态提示、原生消息处理
├── core/application_service.py 窗口刷新、窗口操作、Profile 与恢复流程编排
├── core/window_manager.py     窗口枚举、透明度、置顶、分屏和最大化
├── core/layout.py             布局捕获、窗口匹配、坐标换算和恢复
├── core/config.py             版本化 JSON 配置、原子保存和损坏备份
├── core/hotkeys.py            RegisterHotKey 注册与注销
├── core/models.py             窗口、显示器、矩形和恢复统计模型
└── utils/win_api.py           DPI、DWM 和多显示器 API 封装
```

程序在创建 `QApplication` 前启用 Per-Monitor DPI Awareness V2。`MainWindow` 只收集输入、展示计划和结果；`ApplicationService` 是主窗口、快捷键及托盘入口共用的业务边界，负责调用配置、布局和窗口控制模块并将异常转换为统一 `OperationResult`。GUI 线程负责窗口操作、`WM_HOTKEY` 和 `WM_DISPLAYCHANGE` 消息处理，不使用后台键盘钩子。

## 3. 窗口能力

枚举 `EnumWindows` 返回的可见顶级窗口，并排除以下对象：

- 空标题窗口；
- `WS_EX_TOOLWINDOW` 工具窗口；
- DWM cloaked 隐藏窗口；
- WindowMaster 自身窗口；
- 无法读取进程信息的无效或受保护窗口。

窗口模型包含句柄、标题、PID、可执行文件名、普通状态矩形、所在显示器、最大化和置顶状态。窗口关闭、权限拒绝或句柄失效均转换为状态栏错误，不终止应用。

置顶采用显示器级互斥规则：每个显示器允许零个或一个可管理窗口置顶。新窗口置顶前先取消同一显示器上的全部旧置顶窗口，其他显示器保持不变；任一旧窗口无法取消时不会继续置顶新窗口，从而避免产生新的同屏冲突。

分屏以目标窗口所在显示器的工作区计算，保留任务栏区域并支持位于主显示器左侧或上方的负坐标屏幕。透明度限制为 30%–100%，防止窗口完全不可见。

## 4. 快捷键

使用 Windows `RegisterHotKey` 将消息绑定至主窗口：

|快捷键|操作对象|行为|
|---|---|---|
|`Ctrl+Alt+T`|当前前台窗口；管理界面位于前台时使用列表选中窗口|切换置顶并验证执行后的实际状态|
|`Ctrl+Alt+1`|当前前台窗口|移动到所在屏幕左半区|
|`Ctrl+Alt+2`|当前前台窗口|移动到所在屏幕右半区|
|`Ctrl+Alt+M`|全部可管理程序窗口|逐一最小化窗口|

顶部“快捷键设置”标签打开独立设置窗口，可修改或禁用每个动作的快捷键。保存前校验动作完整性、组合键格式和重复绑定；更新时先注销旧绑定并尝试注册新绑定，注册冲突或配置保存失败时立即回滚旧绑定。程序退出时注销全部快捷键。置顶操作会规范化到可见根拥有者窗口，并在 `SetWindowPos` 后重新读取 `WS_EX_TOPMOST`；应用拒绝或立即重置状态时按失败处理。

## 5. 布局数据与恢复规则

配置路径为 `%APPDATA%\WindowMaster\layouts.json`。当前使用强类型 v2 模式，顶层固定包含 `version`、`profiles` 和 `preferences`：

```json
{
  "version": 2,
  "profiles": {
    "工作": {
      "id": "6b92f6f0-2f12-4eb5-b06e-cb353de1b31a",
      "name": "工作",
      "windows": [
        {
          "id": "5fe7fdc3-88c4-4aad-95aa-254fa8013a43",
          "executable": "Code.exe",
          "title_pattern": "project - Visual Studio Code",
          "match_mode": "exact",
          "rect": [0, 0, 960, 1040],
          "monitor": {
            "device": "\\\\.\\DISPLAY1",
            "work_area": [0, 0, 1920, 1040]
          },
          "maximized": false
        }
      ],
      "restore_policy": {
        "preview": true,
        "auto_restore": false,
        "continue_on_error": true
      },
      "monitor_signature": [
        ["\\\\.\\DISPLAY1", 0, 0, 1920, 1080, 0, 0, 1920, 1040, true]
      ]
    }
  },
  "preferences": {
    "hotkeys": [
      {"action": "toggle_topmost", "shortcut": "Ctrl+Alt+T", "enabled": true}
    ],
    "tray_enabled": true,
    "auto_start": false,
    "display_change_debounce_ms": 1200
  }
}
```

配置使用 UTF-8、嵌套字段校验和临时文件原子替换。读取 v1 文件时先保留 `layouts.v1-backup-时间.json`，再迁移为 v2；备份或写入失败时原文件保持不变。JSON 损坏、编码错误或任一嵌套字段无效时，原文件备份为 `layouts.corrupt-时间.json`，应用回退到空配置。

恢复分为“生成 `RestorePlan`”和“执行计划”两个阶段。计划按保存顺序筛选进程名，并根据 exact、glob、regex 或 process-only 规则匹配未使用窗口；每个当前窗口最多匹配一次。唯一候选进入可执行项，缺失窗口计为跳过，多候选计为冲突，不再静默选择第一个窗口。

计划阶段完成窗口匹配、显示器选择和坐标换算，但不调用任何窗口移动 API。界面展示可执行、跳过和冲突摘要，用户确认后才执行；若确认前后显示器边界、工作区或主屏状态变化，则整次执行在第一个窗口副作用前中止并要求重新预览。

若保存时的显示器仍存在，则根据新旧工作区比例换算坐标；否则迁移到主显示器。最终矩形被限制在工作区内，确保窗口可见。保存时为最大化的窗口先恢复普通矩形，再执行最大化。

## 6. 界面与交互

主界面包含：

- 可按进程名或标题搜索的窗口表格；
- 手动刷新和 2 秒自动刷新；
- 透明度滑块、置顶、左右分屏、最大化按钮；
- 可编辑布局选择框，以及保存/覆盖、恢复、删除按钮；
- 顶部“窗口管理”“快捷键设置”“设置”标签；后两个标签分别弹出对应设置窗口；
- 显示操作成功、快捷键冲突和错误信息的状态栏。

窗口列表刷新后按句柄保留有效选择。未选择窗口时禁用窗口操作。覆盖和删除布局必须二次确认，恢复完成后显示成功、跳过、失败数量。

双击窗口列表中的任意单元格时，界面将该行保存的窗口句柄交给 `ApplicationService`。窗口控制层先规范化到可见根拥有者窗口，仅在最小化状态下执行恢复，然后请求切换到前台并闪烁窗口标题栏和任务栏按钮三次。若 Windows 前台切换策略拒绝激活，仍保留闪烁定位效果并在状态栏说明；该操作不改变窗口位置、大小或置顶状态。

系统托盘默认开启。主窗口顶部“设置”标签和托盘右键菜单均提供常规设置入口，当前设置对话框只包含“关闭时隐藏到系统托盘”开关。启用后，关闭主窗口只隐藏界面，进程、全局快捷键和刷新定时器继续运行；双击图标恢复主窗口，右键菜单提供显示、刷新、设置、按名称选择布局以及明确退出。托盘发起的布局恢复会先显示主窗口，并复用相同的 `RestorePlan` 预览和确认流程。系统不支持托盘时自动关闭并持久化该设置，避免用户关闭窗口后无法找回程序。

程序监听 `WM_DISPLAYCHANGE`，每次事件仅重新启动单次防抖定时器；最后一次事件后等待 `display_change_debounce_ms`，再读取稳定的显示器拓扑。拓扑真正变化时，服务层返回完整签名和匹配布局列表：唯一匹配通过托盘通知进入该布局的预览确认，多个匹配只打开选择界面，没有匹配时提示用户创建或选择布局。通知本身不执行窗口副作用。

新保存布局包含显示器设备名、边界、工作区和主屏状态构成的完整签名。已有 v2 布局缺少签名时，程序使用其窗口规则中的显示器设备名和工作区进行兼容匹配；再次覆盖保存后自动获得完整签名。

## 7. 测试与交付

pytest 测试通过 Mock 隔离 Windows API，覆盖窗口筛选、透明度边界、负坐标分屏、配置损坏备份、一对一窗口匹配、显示器缺失回退、部分窗口缺失恢复、托盘偏好持久化、拓扑签名序列化以及新旧布局匹配。

交付内容包括源码、`requirements.txt`、开发测试依赖、中文 README、测试和 `WindowMaster.spec`。PyInstaller 生成单目录、无控制台窗口程序，`build` 和 `dist` 不纳入源码交付。

人工验收需覆盖主副显示器分屏、负坐标、不同 DPI、快捷键冲突、窗口关闭后刷新，以及高权限窗口被系统拒绝时的错误提示。
